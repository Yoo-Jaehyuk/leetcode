class Solution:
    def maximumWealth(self, accounts):
        max_wealth = 0
        for account in accounts:
            wealth = sum(account)
            if wealth > max_wealth:
                max_wealth = wealth
        return max_wealth
